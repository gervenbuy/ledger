# 記帳系統 v2 更新說明

## 這次新增了什麼

1. **財務健康分數**（新頁籤「財務健康」）
   - 綜合分數 0–100，加燈號（🟢健康 / 🟡注意 / 🔴警示）
   - 四個子指標：儲蓄率(35%)、帳本結構健康度(25%)、成長趨勢(25%)、目標達成度(15%)
   - 「連續2個月支出>收入」的帳本會被標記為結構警示
   - 有去年同期資料時會順便顯示年增率，沒有就略過，不影響分數
   - 附白話文建議（例如哪個帳本該注意、儲蓄率偏低等）

2. **目標設定**（新頁籤「目標設定」）
   - 每個帳本可分別設定當月「營收目標」與「支出上限」
   - 進度條即時對比實際數字
   - 目標會回饋進財務健康分數的「目標達成度」子項

3. 原本的記帳、帳本切換、月份篩選、總覽功能完全保留，操作方式不變。

## 部署步驟（跟你之前操作方式一樣）

1. 打開 Google Sheets 對應的 Apps Script 編輯器
2. **整段刪除**舊程式碼，貼上這次的 `AppsScript_Code.gs`
3. 存檔
4. 部署 →「管理部署作業」→ 點現有部署的鉛筆（編輯）→ 版本選「新版本」→ 部署
   （不要選「新增部署作業」，這樣網址不會變，前端不用改 API_URL）
5. 把這次的 `index.html` 整份覆蓋你 GitHub Pages 上的檔案，重新上傳
6. 打開網頁，密碼已經是你現在用的 `Gerven0530`，登入後應該會看到多出來的「財務健康」「目標設定」兩個頁籤

> 重要：這次程式碼已經內建你現在用的部署網址跟密碼，不用再手動改 `API_URL`。
> Google Sheets 裡原本記錄的資料完全不受影響——這次只是新增程式邏輯跟一個新的「目標設定」分頁，不會動到既有的四個帳本資料。

## 健康分數怎麼算（給你參考，不是使用者需要懂的細節）

- **儲蓄率**：(收入−支出)/收入。0% 給 50 分基準，+30% 以上滿分 100，−30% 以下 0 分。
- **帳本結構**：四個帳本裡，連續兩個月支出>收入的比例，比例越高分數越低。
- **成長趨勢**：本月營收 vs 上月營收成長率，0% 給 50 分，+10% 以上滿分，−20% 以下 0 分。無上月資料時給中性 70 分，不拖累總分。
- **目標達成度**：有設定目標才計分（無目標給中性 70 分）。營收達成率越高分數越高（1.2倍封頂），支出超過上限會扣分。

如果之後用了一陣子覺得某個門檻太嚴或太鬆（例如警示閾值、成長率級距），跟我說一聲，我可以直接調整 `.gs` 裡 `getHealthScore` 那段的參數。

## 之後可以再加的方向（先不做，留個筆記）

- 健康分數歷史趨勢圖（每月分數變化）
- 年度目標（不只月目標）
- 匯出財務健康月報 PDF，方便自己或找記帳士討論
