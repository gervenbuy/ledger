# 工作室財務健康 APP v3 — 部署說明

這是全新架構，跟你之前用的「四個帳本分頁」版本不一樣：現在改成**單一交易紀錄總表 + 業務類型欄位**，可以做應收應付、損益兩平、現金續航、固定成本這些更完整的財務分析。

## 這次有什麼

- **儀表板**：本月營收/支出/淨利/固定成本/現金/損益兩平營收/現金續航月數/健康燈號
- **五大業務表現**：個人、迪特軍EV、電商神助手、課程教學 各自的營收/支出/損益/毛利率
- **快速記帳**：可選業務類型、客戶、專案ID、付款狀態（已收/付、未收/付）
- **帳本**：可搜尋/篩選的交易列表，桌機是表格、手機是卡片，都可以直接「修改」「刪除」
- **未收未付**：應收應付總額 + 待處理明細
- **設定**：現金餘額、老闆月薪目標、固定成本清單管理
- 密碼登入畫面（沿用你原本的密碼 `Gerven0530`）

## 健康燈號怎麼算

- **損益兩平營收** = 本月支出 + 固定成本（你要至少賺到這個數字才不虧錢）
- **現金續航月數** = 現金餘額 ÷ 每月燒錢速度（如果當月是賺錢的，顯示 99 代表沒有燒錢疑慮）
- **燈號**：淨利為正且現金續航 ≥ 3 個月 → 🟢；現金續航 ≥ 1 個月 → 🟡；否則 → 🔴

如果覺得這個門檻太鬆或太嚴，跟我說，我可以調整 `.gs` 裡 `getBootstrapData` 那段參數。

## 部署步驟

### 1. 後端 `.gs`
1. Google Sheets →「擴充功能」→「Apps Script」
2. 全選現有程式碼 → 刪除 → 貼上 `AppsScript_Code_v3.gs` 的內容
3. 存檔
4. 部署 →「管理部署作業」→ 編輯（鉛筆）→ 版本選「**新版本**」→ 部署
   （網址不會變，還是你原本那個 `.../exec`）

### 2. 搬移舊資料（只需要做一次）
1. 存檔後，先**不要部署**，在 Apps Script 編輯器上方函式下拉選單，選 `migrateOldLedgersToTransactions`
2. 按「執行」▶️
3. 第一次執行會跳出權限授權視窗，照指示允許即可
4. 執行完，打開試算表確認多了一個「交易紀錄」分頁，裡面應該有你原本四個帳本的資料
5. 原本的「個人」「迪特軍EV」「電商神助手」「課程教學」四個分頁**不會被刪除**，會保留當備份，之後想封存或刪除都可以自己處理

### 3. 前端 `index_v3.html`
1. 上傳覆蓋你 GitHub Pages 上的 `index.html`（記得改名成 `index.html`，或依你原本的路徑上傳）

### 4. 測試
打開網頁 → 輸入密碼登入 → 應該會看到「儀表板」「快速記帳」「帳本」「未收未付」「設定」五個頁籤，帳本頁籤裡應該看得到搬移過去的舊資料。

## 需要你手動補的東西

- 「設定」頁籤的**現金餘額**、**老闆月薪目標**目前是 0，第一次用的時候記得填一下，不然損益兩平/現金續航的數字不準
- 「固定成本」目前是空的，把你的店租、訂閱費等每月固定支出加進去

## 之前做的「財務健康四維分數」跟「目標設定」頁面

這次沒有帶過來，新設計自己有一套損益兩平/現金續航的邏輯，性質類似但更貼近會計實務。如果之後想把「目標設定」（設定每月營收/支出目標並追蹤進度）加回來，跟我說一聲，我可以另外接進這個新架構裡。
